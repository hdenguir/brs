import React from "react";

import Navbar from "react-bootstrap/Navbar";

const CartNavBar = () => {
  return (
    <Navbar>
      <h2 className="lead">Cart</h2>
    </Navbar>
  );
};

export default CartNavBar;
